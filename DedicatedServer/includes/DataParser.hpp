#include <cstdio>
#include <vector>
#include <string.h>
#include <string>
#include <iostream>

#ifndef DataParser_H
#define DataParser_H

class DataParser
{
private:
    /* data */
public:
    // DataParser(/* args */); // Constructor
    // ~DataParser(); // Destructor

    void Parse(char* stringToParse); // [TODO] Change signature 
};

#endif